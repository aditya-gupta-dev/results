{
  "name": "results",
  "version": "1.0.50",
  "scripts": {
    "client:dev": "bun run client/index.ts",
    "frontend:dev": "cd frontend && bun run dev",
    "frontend:build": "rm -rf dist && cd frontend && bun run build && mv dist/ ../dist",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.ts"
  },
  "dependencies": {
    "@elysiajs/static": "^1.4.7",
    "axios": "^1.13.6",
    "elysia": "latest"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "module": "src/index.js"
}